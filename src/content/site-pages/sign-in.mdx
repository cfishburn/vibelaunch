---
eyebrow: Authentication
title: Sign in
metaTitle: Sign in
description: Clerk's hosted sign-in flow inside the scaffold's app shell.
useContentFlow: true
actions: []
---

The sign-in surface is hosted by Clerk but renders inside the scaffold's layout.

The `@clerk/astro` integration handles the provider automatically — no manual `<ClerkProvider>` needed. The AI guardrails document this as a rule so assistants never add one.
